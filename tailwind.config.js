const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  important: true,
  theme: {
    colors: {
      primary: colors.indigo,
      secondary: colors.pink,
      gray: colors.gray,
    },
  },
};
